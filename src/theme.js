import { createMuiTheme } from "material-ui/styles";
import { deepPurple, amber, red } from "material-ui/colors";

export default createMuiTheme({
  palette: {
    primary: deepPurple,
    secondary: amber,
    error: red
  }
});
